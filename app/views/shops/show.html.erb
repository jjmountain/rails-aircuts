<div class="container">
<div class="row">
    <%= simple_form_for [@shop, @appointment] do |f| %>
      <%= f.input :description %>
      <%= f.input :photo, as: :file %>
      <%= f.input :scheduled_at %>
      <%= f.submit "Book an Appointment", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
